---
tags:
type:
author:
description:
aliases:
date created: Sunday, September 13th 2026, 6:51:42 pm
date modified: Sunday, September 13th 2026, 6:51:44 pm
---
# Equations of Lines and Planes in Space

Two nonzero vectors $u$ and $v$ are parallel if and only if $u=kv$ for some scalar $k$.
$$
\begin{align}
\langle x, y, z \rangle = \langle x_{0}, y_{0}, z_{0} \rangle + t\langle a, b, c \rangle \\
\end{align}
$$
We can also define a set of equations tied to parameter $t$:
$$
\begin{align}
x = x_{0}+ta && y = y_{0} + tb && z = z_{0} + tc
\end{align}
$$
If we solve each of the equations for $t$ assuming $a$, $b$, $c$ are nonzero, we get a different description of the same line:
$$
\begin{align}
\frac{x-x_{0}}{a}=t && \frac{y-y_{0}}{b}=t && \frac{z-z_{0}}{c} = t
\end{align}
$$
The parametric representations are **not unique**. Using a different parallel vector or different point leads to a different, equivalent representation.