Integer model: sorting can be done in $O(n$)

**Model:** Word ram
- Unlimited registers, each can carry $w$-bit word
- Reading/writing/arithmetic/logic on constant number of words takes constant time

Assume all input fits in single word

Unit-cost RAM => all arithmetic is constant, no worries about $w$

Karatsuba multiplication: python on big num

**Static search** (with free pre-processing ie sorting, BST, etc)
- create “dict” where $A[a_i]=i$
- just query in constant time




