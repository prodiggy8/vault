rabdom access -> listening
	no add processing
	people can talk non stop to monopolize
	two people talking at same time!
	works when few people have lots to say
scheduled -> time slots
	extra processing
	non efficient need to stop when time slot is over
	good when multiple ppl have lots to say

carrier send multiple access collision detection


Ethernet -> dominant data link protocol


hidden terminal -> two routers interfering

Ethernet:
listens then send
worse as function of delay, since s sends but takes time to arrive
detects collision:

both wait some time until speaking!
if both wait the same time it will collide again!

then needs to be random between nodes
it will be between range 0-d seconds, d real
d starts low, if collides again then increase

d low means more prob of collision, d high more time waiting and inefficient



UNICAST (1-1) vs MULTICAST (source - groups of nodes) vs BROADCAST (everybody on wire)
broadcast -> read source and if you decide if read or throw away

dyamic addressing: 
static address: cant change, in hardware, ethernet uses that



