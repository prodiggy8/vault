- So far very big and efficient LAN
- Next layer is for a global network

IP is center of hourglass; connects multiple application technologies and network technologies.




- One entry for LAN
- One entry for organization


Class A -> 128 networks (7 bits)

one bit saved for the next range (if starts with 10, it's class B, once it's 110 it's class C)


CIDR

router groups addresses with same options




IP Protocol

source


TTL -> time to live
Header kengnth -> so options can be variable size
Priority (Type Of Serviec) -> how important the packet is


CID